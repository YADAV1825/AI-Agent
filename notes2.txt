Project: AI-Agent

This project is a desktop AI agent designed to automate tasks on the user's computer. It can open applications, browse the web, read and write files, execute shell commands, and run Python code. The goal is to create a flexible, extensible system that can understand natural language instructions and perform them safely and efficiently. Current work includes improving task handling, adding more robust error handling, and expanding the set of supported actions.